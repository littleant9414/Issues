# issues

💎 code runner

```c
  // issue
  $ cd "d:\cc\project\" && gcc hello.c -o hello && "d:\cc\project\"hello
bash: cd: d:\cc\project" && gcc hello.c -o hello && d:ccproject"hello: No such file or directory

  // system 
  windows 10

  // IDE
  Visual Studio Code

  // solution
  🔸 setting.json => "code-runner.terminalRoot": "/"

  🔸 https://github.com/formulahendry/vscode-code-runner/issues/296

```
